---
const locale = "en";
/** 
 * With the template literal `astro check` gives the following incorrect warning:
 * 
 * ```
 * src/pages/index.astro:2:7 - warning ts(6133): 'locale' is declared but its value is never read.
 * ```
 */
return Astro.redirect(`/${locale}`, 307);
---